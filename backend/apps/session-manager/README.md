# 🏫 Session Manager Microservice

Controls the lifecycle of active classroom sessions.

### Functions:
- Validates the Time-to-Live (TTL) of generated QR codes (default: 15 min).
- Manages session status (Open/Closed).
- Returns metadata about the physical classroom and assigned teacher.